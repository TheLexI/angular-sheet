import { ContentRefDirective } from './content-ref.directive';

describe('ContentRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentRefDirective();
    expect(directive).toBeTruthy();
  });
});
