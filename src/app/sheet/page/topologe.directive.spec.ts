import { TopologeDirective } from './topologe.directive';

describe('TopologeDirective', () => {
  it('should create an instance', () => {
    const directive = new TopologeDirective();
    expect(directive).toBeTruthy();
  });
});
