import { SubmenuRefDirective } from './submenu-ref.directive';

describe('SubmenuRefDirective', () => {
  it('should create an instance', () => {
    const directive = new SubmenuRefDirective();
    expect(directive).toBeTruthy();
  });
});
